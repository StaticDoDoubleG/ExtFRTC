import 'dart:typed_data';

class HashUtil {
  static Future<String> sha256Hex(Uint8List data) async => throw UnimplementedError();
  static Future<bool> verify(Uint8List data, String expectedHex) async => throw UnimplementedError();
}
